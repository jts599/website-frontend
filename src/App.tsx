import './App.css'

function App() {
  return (
    <main className="app">
      <section className="hero">
        <p className="eyebrow">React + TypeScript starter</p>
        <h1>website-frontend</h1>
        <p className="intro">
          This frontend is set up for local development in a dev container and can be
          deployed to GitHub Pages from the repository workflow.
        </p>
      </section>
    </main>
  )
}

export default App
